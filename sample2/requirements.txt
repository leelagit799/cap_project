# DischargeFlow — dependencies
# Every entry maps to a component named in
# FA5_SP_Interns_Capstone_AI_Discharge_Summaries.docx, Table 14 (Technology Stack).

# --- Agent frameworks (doc Table 6: LangGraph / Google ADK / Agno) ---
langgraph==1.1.10
google-adk==2.6.2
agno==2.3.21

# --- Protocols: MCP (dual server) and A2A ---
mcp==1.25.0
mcp-use==1.7.0
a2a-sdk==0.3.22

# --- Web services: Mock EHR :8050, MCP servers, A2A servers ---
# google-adk 2.6.2 requires fastapi>=0.133, so the original 0.123.10 pin cannot hold.
fastapi>=0.133,<1
uvicorn==0.40.0
httpx
sse-starlette

# --- User interfaces (doc Table 14: Streamlit HITL :8501, Gradio host :8083) ---
streamlit==1.52.2
# gradio 6.2.0 pins starlette<1.0, which google-adk 2.6.2 (starlette>=1.3.1) rules out.
gradio>=6.22,<7
plotly

# --- LLM gateway and models (Bedrock Nova Lite primary, Cohere Command R+ fallback) ---
litellm==1.80.11
boto3

# --- Retrieval: FAISS vector store + embeddings ---
faiss-cpu==1.13.2
sentence-transformers==5.2.2
numpy

# --- Document parsing: TXT / JSON / PDF / DOCX / PNG-OCR ---
pdfplumber
pypdf
python-docx
pytesseract
Pillow
langdetect

# --- Reporting: JSON + HTML + PDF audit reports ---
jinja2
weasyprint

# --- Observability ---
langfuse>=4.0,<5

# --- Configuration and data ---
PyYAML==6.0.3
pydantic==2.12.5
python-dotenv
pandas==2.3.3

# --- Testing ---
pytest
pytest-asyncio
